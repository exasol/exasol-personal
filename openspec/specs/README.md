# OpenSpec Specs

This directory is intentionally minimal right now.

Permanent capability specs are expected to appear here after approved changes from `openspec/changes/` are implemented and archived.
