# OpenSpec

This repository uses OpenSpec-style change proposals under `openspec/changes/`.

The current proposal for local deployment lives at:

- `openspec/changes/add-local-deployment/`

The permanent capability specs under `openspec/specs/` are expected to be populated when approved changes are implemented and archived.
