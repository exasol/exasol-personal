## 1. Documentation foundation

- [x] 1.1 Add the co-located locked documentation dependencies, minimal Material for MkDocs site,
  mike redirect-alias configuration, and local Task command; verify a strict local build and commit
  the independently working foundation.

## 2. Manual publication

- [x] 2.1 Add local version-catalog management and the manually dispatched publish/delete workflow
  with semantic-tag validation, single-push mike updates, serialized execution, pinned actions,
  separated job permissions, and GitHub Pages deployment; validate the workflow and full relevant
  repository checks and commit it.

## 3. Maintainer guidance

- [x] 3.1 Document local documentation validation, required GitHub Pages setup, manual publish and
  delete inputs, protected-default deletion behavior, and failure recovery; validate documentation
  links and full relevant repository checks and commit the guidance.

## 4. Specification closeout

- [x] 4.1 After implementation validation is complete, archive the OpenSpec change, validate the
  archived specification, and commit the OpenSpec artifacts separately from implementation files.
