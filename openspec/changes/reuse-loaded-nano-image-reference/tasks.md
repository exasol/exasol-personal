## 1. Podman Image Lifecycle

- [ ] 1.1 Remove the deployment-specific Nano image tag and verify the run command uses the loaded reference directly
- [ ] 1.2 Update fake-Podman tests for named and ID references, removed tag failures, and the shortened command sequence; verify the targeted package tests pass

## 2. Verification

- [ ] 2.1 Format the implementation and verify the OpenSpec change passes strict validation
- [ ] 2.2 Run the repository unit tests, lint, and build successfully
